# disguise remote cue frontend

Non-functional Vue/Vite prototype for a phone and tablet-friendly disguise operator interface.

## Run

```sh
npm install
npm run dev
```

The initial design export is kept in `design-reference/`.
